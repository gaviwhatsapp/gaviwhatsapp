# Send a WhatsApp Message (Python)

Send a text message using the Gavi WhatsApp Python SDK.

## Setup

```bash
pip install -r requirements.txt
```

## Run

```bash
export GAVIWHATSAPP_API_KEY=gv_your_key
python main.py
```

Edit `main.py` to change the recipient number and message text.
