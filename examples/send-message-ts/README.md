# Send a WhatsApp Message (TypeScript)

Send a text message using the Gavi WhatsApp TypeScript SDK.

## Setup

```bash
npm install
```

## Run

```bash
export GAVIWHATSAPP_API_KEY=gv_your_key
npm start
```

Edit `index.ts` to change the recipient number and message text.
